#include <string>
#include <iostream>
#include <sstream>
#include "Functions.h"

int string_date_to_int(string s)
{/*
    int bday[3], i = 0;
    string v;
    stringstream st(s);
    while (getline(st, v, '/')) bday[i++] = stoi(v);
    st.clear();
    int year = 2016;
    int month = 12;
    int day = 30;
    st << "Лет" << year - bday[2] << " Месяцев-" << month - bday[1] << " Дней " << day - bday[0] << endl;
    return st.str();*/
}
